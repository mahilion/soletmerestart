#include<stdio.h>
int main(void)
{
int a;//a memory is created for a in stack
printf("a is %d before initialisation\n",&a);
a=10;//initialise a with 10;
printf("after initialisation a is %d\n ",&a);
printf("done\n");
return 0;
}
